<div>
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<ul class="navbar-nav mr-auto mt-2 mt-lg-0">
			<li class="nav-item">
				<a class="nav-link" href="/">Home</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('artist_index') }}">Add new artist</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('album_index') }}">Add new album</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('song_index') }}">Add new song</a>
			</li>
		</ul>
		{% if not app.user %}
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link btn btn-outline-success mr-3" href="{{ path('app_login') }}">Login</a>
				</li>
				<li class="nav-item">
					<a class="nav-link btn btn-outline-success mr-3" href="{{ path('app_register') }}">Register</a>
				</li>
			{% endif %}
		</ul>
		{% if app.user %}
			<ul class="navbar-nav">
				<li class="nav-item text-white">You are logged in as
					{{ app.user.email }}
					<a class="btn btn-danger" href="{{ path('app_logout') }}">Logout</a>
				</li>
			</ul>
		{% endif %}
	</nav>
</div>
